<h1><%= link_to 'QB Dash', root_url %></h1>

<% if session[:user_id] %>
  <%= link_to 'Dashboard', dashboard_url %> | 
  <%= link_to 'Sign out', signout_url %>
  <ipp:blueDot></ipp:blueDot>
<% else %>
  <ipp:login href="<%= root_url %>oauth_request"></ipp:login>
<% end %>